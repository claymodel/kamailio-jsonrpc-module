* Implement Timeouts (timerfd)
* Handle OOM errors on malloc
* Blackhole responses for cancelled transactions (perhaps send to error route?)
* "Reliable" Notifications (implement using requests, and failover on error; ignore non-error responses) ?
